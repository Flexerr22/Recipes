import { cn } from "@/lib/utils"

interface Props {
    className?: string
}

export const Catalog: React.FC<Props> = ({className}) => {
    return (
        <div className={className}>
            
        </div>
    )
}